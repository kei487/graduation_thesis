\chapter{シミュレータ実験}\label{chap:experiment}
%もっかい実験して結果の図とかちゃんと載せたい

%\section{シミュレータ実験}

提案手法の走り出しまでの時間を短縮する効果の有無を
検証するために，シミュレータを用いて実験を行う．
実験を行うシミュレータ環境は，動的な障害物が存在せず，
障害物回避による移動時間の増加を考慮する必要がない
大域計画による移動時間を計測する理想的な環境である．

\section{実験条件}
シミュレータ実験では，
千葉工業大学津田沼キャンパスで得られた
図\ref{fig:map_nav}の地図を使用する．
紙面横方向が300m，縦方向が200mの6haの環境の地図で，
形式は，ROSのナビゲーションスタックで用いられる
占有格子地図である．
白色が障害物のない画素，
黒色が障害物のある画素,灰色が不明な画素を表す.
この地図のパラメータは表\ref{tab:map_info}の通りである．

価値反復ROSパッケージでは，地図のグリッドに加え，
$\theta$方向にも離散化を行い$S$を構成する．
$S$及び価値反復ROSパッケージの諸元を表\ref{tab:vi_info}に，
行動のリストを表\ref{tab:vi_action}に示す．
状態の数は1億に達し，移動可能なエリアだけでも1千万に達する．

\begin{figure}[htb]
  \centering
  \includegraphics[width=0.8\linewidth]{figs/map_nav.eps}
  \caption{a map for navigation}
  \label{fig:map_nav}
\end{figure}

\begin{table}[bt]
    \centering
    \caption{Configurations of the Map}
    \label{tab:map_info}
	\begin{small}
    \begin{tabular}{l|l}
        \hline
        map size & 294.6[m]$\times$199.95[m] \\
        cell resolution & 0.15[m]$\times$0.15[m] \\
        number of cells & 2,615,346 \\
        number of free cells & 165,076 \\
        the area of the free cells & 3714.98[m$^2$] \\
        \hline
    \end{tabular}
	\end{small}
\end{table}

\begin{table}[bt]
    \centering
    \caption{List of Action in Value Iteration}
    \label{tab:vi_action}
	\begin{small}
    \begin{tabular}{l|cc}
        \hline
	Type & Linear Velocity [m/s] & Angular Velocity [deg/s] \\
        \hline
	forward & 0.3 & 0.0 \\
	back & -0.2 & 0.0 \\
	right & 0.0 & -20.0 \\
	right forward & 0.2 & -20.0 \\
	left & 0.0 & 20.0 \\
	left forward & 0.2 & 20.0 \\
        \hline
    \end{tabular}
	\end{small}
\end{table}

\begin{table}[bt]
    \centering
    \caption{Parameters of Value Iteration}
    \label{tab:vi_info}
	\begin{small}
    \begin{tabular}{l|l}
        \hline
	$\theta$-axis status resolution & 6 deg \\
        number of states & 156,920,760 \\
	number of free states & 9,904,560 \\
	number of actions & 6 \\
	$\Delta$t & 1 \\
        \hline
    \end{tabular}
	\end{small}
\end{table}


実験で使用するシミュレータは，
図\ref{fig:map_loc}から作成したシミュレータを使用する．
シミュレータには，ROSの標準的なシミュレータである
Gazebo\cite{Gazebo}を使用し，
シミュレータ内で用いる静的な障害物の作成には，
占有格子地図から作成するプログラム\cite{池邉2024sim}
を使用した．

\begin{figure}[htb]
  \centering
  \includegraphics[width=0.8\linewidth]{figs/map_loc.eps}
  \caption{a map for localization}
  \label{fig:map_loc}
\end{figure}

シミュレータ内で走行させるロボットは，
図\ref{fig:raspicat}
の差動二輪型のロボットである．
差動二輪型のロボットは，
進行方向に対して前後に進むことができるが，
横に進むことができない．
そのため，当然ながらゴールの方向へ進むには
その方向かその反対を向く必要がある．
つまり，行動には向きの制限がつくため，
位置と方向の3次元で経路を探索することで
移動の効率化が計れる．

% この写真微妙なのであとで差し替える
\begin{figure}[htb]
  \centering
  \includegraphics[width=0.8\linewidth]{figs/raspicat.eps}
  \caption{a robot for simlation}
  \label{fig:raspicat}
\end{figure}


実験に用いた計算機は，CPUとしてRyzen 9 7945HX3D（16コア32スレッド），
DRAMとしてDDR5-4800 32GBが2枚の合計64GB搭載されたものである．
価値反復ROSパッケージは，CPUですべて計算するため，
コアの数が重要である．
このコンピュータは，現代の家庭用コンピュータの中でも，
コアの数の多いCPUを搭載している．
DRAMについては，地図の大きさに応じて決定した．

% スレッド数とゴール設定，比較対象は変更して実験をし直したい
% 大域計画だけにスレッドを割り振り
% 比較対象にA*経路にただ追従するものを追加して
価値反復ROSパッケージは，計算の効率化のため並行に$V$を計算できるよう
実装されている．
環境の全域で$V$を計算する大域計画器と，
ロボットの周囲で$V$を計算する局所計画器に任意のスレッドの数
を割り当てることができる．
本稿の実験では，大域計画器に8，
局所計画器に6のスレッドを割り当てた．
また，A*は別のプロセスで動作し，
CPUから見ると，A*は1つのスレッドである．
また，シミュレータのGazeboにも計算機のリソースを使用している．

実験では，図\ref{fig:map_nav}に示す3つの目的地で比較を行った．
Goal1は，式（\ref{eq:2d-astar-heurisic}）
あるいは式(\ref{eq:3d-astar-heurisic})のヒューリスティック関数
を使ったときにすぐ経路が見つかるように設定した．
同様に，Goal2は，障害物に阻まれ，大きな迂回が必要なように，
Goal3は，旋回の所要時間を考慮しない式（\ref{eq:2d-astar-heurisic}）
のA*が不利になるように，
スタートでのロボットの向きと反対方向に設定した．
Goal2に迂回が必要となるのは，
図\ref{fig:map_nav}右の丸で囲ったところやその他数カ所に，
移動ロボットが跨げない段差が存在するからである．

比較するプログラムは，価値反復ROSパッケージ単体（VI），
2D A*と価値反復ROSパッケージを併用したもの（VI+2D A*），
3D A*と価値反復ROSパッケージを併用したもの（VI+3D A*）の3つである．
これとは別に，2D A* の経路をpure pursuitで追従したもので
%pure pursuitの説明が，ない！
移動時間を計測し，ロボットが移動するのにかかる時間を計測する．

\section{実験結果}
VI，VI+2D A*，VI+3D A*での
ロボットが移動に要する時間を比較した．
5回ずつ試行した総移動時間を表\ref{tab:result}に示す．
また，2D，3DのA*が暫定経路の算出にかけた時間
を表\ref{tab:result_calc}に，
Goal1, 2までの
2D A*の経路を追従した移動時間を表\ref{tab:result_track}に示す．

Goal1では，VI+2D A*とVI+3D A*のどちらの移動時間も，
VIの移動時間に比べ，減少しており，
移動時間短縮の効果を確認できる．
また，表\ref{tab:result_calc}から，
A*の計算時間と移動時間を比較して
移動時間の1.5\%以内の時間で，
A*の計算が終了していることが分かる．

Goal3では設置の意図通り，
VI+3D A*が総移動時間の最も小さくなっており，
A*を3Dにする効果が確認できている．
計算時間は，VI+2D A*に比べ大きいが，
総移動時間は，小さくなっているのは，
VI+2D A*の勾配の作り方が，角度方向はVI
によって作られているため，その計算に時間がかかったためと考えられる．

Goal2については，VI+2D A*とVI+3D A*のどちらとも
VIより大幅に時間がかかった．
図\ref{fig:result_path}に示すように，
A*は最短の経路を見つけていたが，
価値反復の計算の途中で$V$の勾配に分岐が生じ，
ロボットが迷走するという現象が見られた．
表\ref{tab:result_g2}に，Goal2までの
VI+2D A*とVI+3D A*の総移動時間，A*の計算時間，
（2D A*の経路の）移動時間と総移動時間から計算時間と移動時間を
引いたものを示す．

また，3D A*の計算時間は，Goal1-3で，
2D A*の計算時間よりも大きくなった．
Goal3では，3D A*の経路は，2D A*の経路よりも
効率的な経路であったが，
探索空間が大きくなり，
計算時間が増加する影響が移動時間よりも大きい
Goalもあった．


上記のロボットが迷走する問題の対策については，
A*探索を定期的に何度も実行する方法が考えられる．
そうすることで$V$に分岐が生じた際，
その状態からゴールまでの別の経路をA*が発見できる可能性がある．
ただし，価値反復が計算した正確な$V$を上書きして変更する
可能性もあるため，式（\ref{eq:propose}）
での勾配のつけかたに工夫が必要とされる．



\begin{table}[htb]
    \centering
    \caption{Result with the Map of Outdoor Environment}
    \label{tab:result}
    \begin{tabular}{l|ccc}
        \hline
	    & \multicolumn{3}{c}{traveling time (standard deviation)[s]} \\
            methods & Goal1 & Goal2 & Goal3 \\
        \hline
            VI & 226 (7) & \, 597 (1)\phantom{00}  & 62 (7) \\
	    VI$+$2D A* & 214 (1) & 1135 (129) & 69 (2) \\
            VI$+$3D A* & 219 (1) & 1414 (169) & 54 (4) \\
        \hline
    \end{tabular}
    \centering
    \caption{Calculation Time for Temporal Path}
    \label{tab:result_calc}
    \begin{tabular}{c|ccc}
        \hline
	    & \multicolumn{3}{c}{calculation time (standard deviation)[s]} \\
            methods & Goal1 & Goal2 & Goal3 \\
        \hline
	    2D A* & 2.3 (0.3) & \ 27 (0.1) & 2.4 (0.1) \\
            3D A* & 2.5 (0.2) & 189 (8.5) & 2.8 (0.1) \\
        \hline
    \end{tabular}
    \centering
    \caption{Travering Time for Temporal Path}
    \label{tab:result_track}
    \begin{tabular}{c|cc}
        \hline
	    & \multicolumn{2}{c}{travering time (standard deviation)[s]} \\
            methods & Goal1 & Goal2  \\
        \hline
            2D A* & 204 (0) & 571 (1)  \\
        \hline
    \end{tabular}
    \centering
    \caption{Time for Goal 2}
    \label{tab:result_g2}
    \begin{tabular}{c|cccc}
        \hline
	    & \multicolumn{4}{c}{times for Goal 2 (standard deviation)[s]} \\
            Methods & All & Calculate & Moving & Other \\
        \hline
            2D A* & 1135(129) & 27(0.1)  & 571 (1) & 537(130) \\
            3D A* & 1414(169) & 189(8.5) & 571 (1) & 654(179) \\
        \hline
    \end{tabular}
\end{table}


\begin{figure}[htb]
  \centering
  \includegraphics[width=0.8\linewidth]{example-image-16x9.pdf}
  \caption{A temporal path from 2D A*}
  \label{fig:result_path}
\end{figure}

% 実機で実験したい．
% \section{実機実験}


